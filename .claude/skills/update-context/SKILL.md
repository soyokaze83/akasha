---
name: update-context
description: Regenerate .claude/CONTEXT.md with comprehensive, up-to-date technical documentation of the entire Akasha project
disable-model-invocation: true
---

# Update Project Context

Regenerate the `.claude/CONTEXT.md` file with a comprehensive technical description of the Akasha project. This file serves as a living document describing what the project does and how it's built — distinct from `CLAUDE.md` which contains instructions for Claude.

## Current Project Structure

!`find src/ -type f -name "*.py" | sort`

## Current Dependencies

!`cat pyproject.toml`

## Current Docker Setup

!`cat docker-compose.yml`

## Instructions

Read through ALL of the following files to build a complete understanding of the project. Do not skip any — each contains important context:

**Entry point & webhook routing:**
- `src/main.py`

**Core infrastructure (read every file):**
- `src/core/config.py`
- `src/core/logging.py`
- `src/core/scheduler.py`
- `src/core/rate_limiter.py`
- `src/core/background_tasks.py`
- `src/core/gowa/client.py`
- `src/core/gowa/models.py`

**LLM abstraction layer (read every file):**
- `src/llm/base.py`
- `src/llm/gemini.py`
- `src/llm/openai.py`
- `src/llm/openrouter.py`
- `src/llm/key_rotator.py`

**Services (read every file in each service):**
- `src/services/mandarin_generator/` — all .py files
- `src/services/reply_agent/` — all .py files
- `src/services/chat_summarizer/` — all .py files

**Utilities:**
- `src/utils/web_scraper.py`

**Deployment:**
- `Dockerfile`

Use parallel reads where possible to speed things up.

After reading all files, write `.claude/CONTEXT.md` with the following structure. Be thorough but concise — focus on what each component actually does, not boilerplate descriptions. Include specific details like class names, method signatures, trigger phrases, temperature values, retry counts, etc. that are useful for understanding the implementation.

## CONTEXT.md Structure

```markdown
# Akasha — Project Context

> Auto-generated by `/update-context`. Last updated: {current date YYYY-MM-DD}

## Project Overview
<!-- What Akasha is, its business purpose, who uses it -->

## Architecture
<!-- High-level system design: FastAPI app + GoWA, how they connect -->
<!-- Include a text diagram showing the component relationships -->

## Services

### Mandarin Generator
<!-- Purpose, generation modes (free/hackernews/specific), HSK level targeting -->
<!-- HackerNews top story selection, passage validation, scheduling -->
<!-- Trigger: scheduled daily + manual API endpoint -->

### Reply Agent
<!-- Purpose, trigger mechanisms ("hey akasha," prefix, reply-to-akasha) -->
<!-- Tool calling orchestration loop, state machine (NEEDS_TOOL_CALL/INTERMEDIARY/FINAL_ANSWER) -->
<!-- DSPy intermediary classification, max iterations, tool call limits -->
<!-- Multimodal support (image understanding), quoted message context -->
<!-- Provider fallback chain with specific error codes that trigger fallback -->

### Chat Summarizer
<!-- Purpose, trigger phrase pattern, message count extraction -->
<!-- Display name resolution (standard JID vs LID format) -->
<!-- Language auto-detection for summary output -->

## Core Infrastructure

### Configuration
<!-- Pydantic Settings, .env loading, key properties (gemini_api_keys, recipients_list) -->

### GoWA Client
<!-- WhatsApp API wrapper, retry logic, media download fallback chain -->
<!-- Pagination for chat messages, health check -->

### Webhook Processing
<!-- Signature verification, rate limiting, deduplication (own messages + processed) -->
<!-- Event type detection, media path caching -->
<!-- Background task dispatch pattern (return 200 immediately) -->

### Scheduler
<!-- APScheduler jobs: daily passage + cache cleanup -->
<!-- Timezone handling, cleanup targets -->

### Rate Limiter
<!-- Sliding window algorithm, per-sender tracking, async-safe -->

## LLM Layer

### Provider Abstraction
<!-- LLMClient Protocol, factory function, singleton pattern -->
<!-- Multimodal method signature (image_data, image_mime_type) -->

### Gemini Client
<!-- Key rotation on quota/rate/server errors, multimodal Content building -->
<!-- Rotation triggers: 429, quota, rate, exhausted, 503, 500, invalid key -->

### OpenAI Client
<!-- Base URL configuration, base64 image encoding for vision -->

### OpenRouter Client
<!-- Text-only fallback, no vision support, warning on image data -->

### Fallback Chain
<!-- Primary provider -> fallback on 429/503/quota errors -->
<!-- Image data stripped for OpenRouter fallback -->

## API Endpoints
<!-- Table with Method, Path, Description for all routes -->

## Webhook Message Flow
<!-- Step-by-step: signature verify -> rate limit -> extract message -> deduplicate -> route to service -> background task -> return 200 -->

## Key Technical Patterns
<!-- Singleton services, lazy initialization, async background tasks -->
<!-- Retry with exponential backoff, key rotation, concurrent send semaphore -->
<!-- Message ID tracking (own messages, processed, media paths) with TTL cleanup -->
<!-- DSPy for LLM-based classification -->

## Deployment
<!-- Docker Compose: akasha + whatsapp (GoWA) + optional qdrant -->
<!-- Dockerfile: python:3.12-slim, uv for deps, port 8080 -->
<!-- Environment variable groups summary -->
```

Write the complete CONTEXT.md to `.claude/CONTEXT.md`. Fill in every section with real details from the code you read — no placeholders or TODOs. Use the actual class names, method names, constants, trigger phrases, temperature values, retry counts, and error codes found in the source code.
