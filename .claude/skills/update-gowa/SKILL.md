---
name: update-gowa
description: Regenerate .claude/GOWA_INTEGRATION.md by reading the actual GoWA client code, webhook handling, and docker config — use when GoWA integration issues arise (incompatible properties, API changes, media download errors, webhook payload mismatches)
disable-model-invocation: true
---

# Update GoWA Integration Documentation

Regenerate `.claude/GOWA_INTEGRATION.md` with accurate, current documentation of how Akasha integrates with GoWA (go-whatsapp-web-multidevice) for WhatsApp connectivity. This document is the single source of truth for GoWA API behavior, webhook payload formats, and troubleshooting — it must match the actual implementation.

## Current Docker Configuration

!`cat docker-compose.yml`

## Instructions

Read through ALL of the following files. Each one contains GoWA integration logic:

**GoWA client implementation:**
- `src/core/gowa/client.py` — GowaClient class with all API methods, retry logic, error handling
- `src/core/gowa/models.py` — Pydantic models for webhook payloads and API responses

**Webhook handling:**
- `src/main.py` — Webhook endpoint: signature verification, event type detection, message ID extraction, media path caching, deduplication, service routing

**GoWA usage in background tasks:**
- `src/core/background_tasks.py` — How gowa_client.send_message() is called, error response formatting

**Configuration:**
- `src/core/config.py` — GoWA-related settings (gowa_base_url, gowa_username, gowa_password, gowa_webhook_secret)

Use parallel reads where possible to speed things up.

After reading all files, write `.claude/GOWA_INTEGRATION.md` with the structure below. Be precise — use the exact method signatures, endpoint paths, retry counts, backoff timers, field names, and error codes from the source code. This document is used to debug GoWA issues, so accuracy matters more than prose.

## GOWA_INTEGRATION.md Structure

```markdown
# GoWA Integration Guide

> Auto-generated by `/update-gowa`. Last updated: {current date YYYY-MM-DD}

This document serves as a knowledge base for Claude and developers working on the Akasha project. It documents the GoWA (go-whatsapp-web-multidevice) service integration.

## Project Overview
<!-- Brief: Akasha is a FastAPI platform integrating with WhatsApp via GoWA -->

### Architecture
<!-- ASCII diagram showing: Akasha (FastAPI) services → Shared Core (GoWA Client) → GoWA Service (Docker) -->
<!-- Show the HTTP port, webhook callback direction, Docker network -->

## GoWA Service Summary
<!-- What GoWA is, Docker image name and version, default config table (port, auth, database path) -->

## Key API Endpoints

<!-- All endpoints require Basic Auth. Document each endpoint the GowaClient actually calls: -->

### Authentication & Device Management
<!-- GET /app/devices (check_health, get_devices), GET /app/login -->

### Sending Messages
<!-- POST /send/message — exact payload format {phone, message, reply_message_id} -->
<!-- Include request and response JSON examples with actual field names from code -->
<!-- Document retry logic: attempt count, backoff range, which errors trigger retry -->

### Media Operations
<!-- GET /message/{message_id}/download?phone={phone} — download_media() method -->
<!-- Document the 3-layer fallback: webhook file_path → cached media_file_paths → on-demand API download -->
<!-- GET /{file_path} — download_media_from_path() for auto-downloaded files -->
<!-- Document retry logic for media: attempt count, wait time -->
<!-- Document the JSON response edge case (file path extraction from "Media downloaded successfully to {path}") -->

### Chat Operations
<!-- GET /chat/{chat_jid}/messages?limit={limit}&offset={offset} -->
<!-- Document pagination logic: GOWA_MAX_MESSAGES_LIMIT constant, how limits > 100 are handled -->

### User Info
<!-- GET /user/info?phone={phone_jid} — get_user_info() method -->
<!-- Document retry logic, response fields (verified_name, status, picture_id) -->

## Phone Number Formats (JID)
<!-- Individual: {country}{phone}@s.whatsapp.net -->
<!-- Group: {group_id}@g.us -->
<!-- Group sender compound format: {phone}:{device_id}@s.whatsapp.net in {group_id}@g.us -->
<!-- Include parsing logic code example from main.py (split by " in ", strip device ID) -->

## Webhook Integration

### Configuration
<!-- Docker-compose environment variables for webhook URL, secret, timeout -->

### Webhook Payload Structure
<!-- Document WebhookPayload model fields: sender_id, chat_id, from_jid (alias "from"), timestamp, pushname, type, message -->
<!-- Document WebhookMessage model fields: text, id, replied_id, quoted_message -->

### Event Type Detection
<!-- How main.py determines event type by payload structure (not the type field) -->
<!-- reaction field → reaction, isinstance(image, dict) → image, message.text → text, else → other -->

### Event Types Table
<!-- All known event types with their additional fields -->

### Reply Message Fields
<!-- replied_id, quoted_message, file_path — with example JSON payload -->

### Image Message Webhook
<!-- Payload structure with image object (url, caption, mime_type) -->
<!-- Processing flow: receive → extract ID → download via API → process with LLM -->

### Signature Verification
<!-- HMAC-SHA256 via X-Hub-Signature-256 header -->
<!-- How main.py verifies: skips if default/unconfigured secret, returns 401 on mismatch -->

## Webhook Processing Pipeline
<!-- Step-by-step flow from main.py: -->
<!-- 1. Signature verification -->
<!-- 2. Rate limit check (per sender_jid) -->
<!-- 3. Message ID extraction (different paths for media vs text) -->
<!-- 4. Duplicate prevention: skip if in akasha_message_ids (own) or processed_message_ids -->
<!-- 5. Lock-based TOCTOU protection -->
<!-- 6. Event type detection -->
<!-- 7. Media path caching -->
<!-- 8. Service routing (Reply Agent text/image, Chat Summarizer) -->
<!-- 9. Background task dispatch via asyncio.create_task() -->
<!-- 10. Return {"status": "ok"} immediately -->

## Message ID Tracking
<!-- Three caches: akasha_message_ids, media_file_paths, processed_message_ids -->
<!-- MESSAGE_ID_TTL = 86400 (24h), cleanup via scheduler every 30 minutes -->

## Background Task GoWA Usage
<!-- How process_text_reply_background and process_image_reply_background call gowa_client.send_message() -->
<!-- Reply threading via reply_message_id parameter -->
<!-- Error response functions: _send_error_response, _send_image_error_response -->
<!-- Error-specific user messages (503, 429, timeout, API key issues) -->

## GowaClientError
<!-- Custom exception class, when it's raised, how callers handle it -->

## Docker Setup
<!-- docker-compose services: akasha depends_on whatsapp -->
<!-- GoWA image version, ports, volumes, environment variables -->
<!-- Authentication flow: start services → GET /app/login → scan QR → verify with /app/devices -->
<!-- Session persistence via whatsapp_data volume -->

## Important Notes
<!-- WhatsApp Web protocol (not official Business API), works with personal accounts -->
<!-- Session maintenance: phone must come online every 14 days -->
<!-- Webhook timeout: must be >= 60s for image processing (LLM vision takes 15-30s) -->
<!-- Duplicate webhook handling: GoWA retries on timeout, mark processed immediately -->

## Troubleshooting

### Image Not Responding
<!-- Symptoms, common causes (message ID extraction fail, webhook timeout, duplicate processing) -->
<!-- Solutions with specific config values -->
<!-- Expected logging patterns in order (message ID found → trigger detected → download → LLM → response sent) -->
<!-- Diagnosis checklist -->

### Common Errors
<!-- GowaClientError patterns, "you are not logged in", session expired -->
<!-- Media download failures: JSON response edge cases, file path extraction -->
```

Write the complete GOWA_INTEGRATION.md to `.claude/GOWA_INTEGRATION.md`. Fill in every section with real details from the code you read. Use exact method names, field names, constants, retry counts, endpoint paths, and error messages found in the source. No placeholders or TODOs.
